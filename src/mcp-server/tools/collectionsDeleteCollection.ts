/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsDeleteCollection } from "../../funcs/collectionsDeleteCollection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCollectionRequest$inboundSchema,
};

export const tool$collectionsDeleteCollection: ToolDefinition<typeof args> = {
  name: "collections-delete-collection",
  description: `Delete an existing collection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsDeleteCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
