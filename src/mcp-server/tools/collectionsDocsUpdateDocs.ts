/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsDocsUpdateDocs } from "../../funcs/collectionsDocsUpdateDocs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateDocsRequest$inboundSchema,
};

export const tool$collectionsDocsUpdateDocs: ToolDefinition<typeof args> = {
  name: "collections-docs-update-docs",
  description:
    `Update documents in a collection. Note that the maximum supported payload size is 6MB.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsDocsUpdateDocs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
