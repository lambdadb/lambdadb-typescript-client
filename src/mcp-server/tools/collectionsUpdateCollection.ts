/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsUpdateCollection } from "../../funcs/collectionsUpdateCollection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCollectionRequest$inboundSchema,
};

export const tool$collectionsUpdateCollection: ToolDefinition<typeof args> = {
  name: "collections-update-collection",
  description: `Configure a collection.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsUpdateCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
