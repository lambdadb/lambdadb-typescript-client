/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsDocsGetBulkUpsertDocs } from "../../funcs/collectionsDocsGetBulkUpsertDocs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBulkUpsertDocsRequest$inboundSchema,
};

export const tool$collectionsDocsGetBulkUpsertDocs: ToolDefinition<
  typeof args
> = {
  name: "collections-docs-get-bulk-upsert-docs",
  description: `Request required info to upload documents.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsDocsGetBulkUpsertDocs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
