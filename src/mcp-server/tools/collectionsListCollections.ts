/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsListCollections } from "../../funcs/collectionsListCollections.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$collectionsListCollections: ToolDefinition = {
  name: "collections-list-collections",
  description: `List all collections in an existing project.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await collectionsListCollections(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
